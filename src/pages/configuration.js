import React from 'react';
import Navbar from '../Components/NavBar';
import Footer from '../Components/Footer';

const configuration = () => {
    return (
        <div>
           <Navbar/>
            I am from the Configuration Page
            <Footer/>
        </div>
    );
};

export default configuration;